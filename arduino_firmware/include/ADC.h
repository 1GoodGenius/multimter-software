#ifndef ADC_H
#define ADC_H

// Minimal stub to satisfy projects that include <ADC.h> on platforms
// that don't provide a separate ADC library (AVR uses analogRead/registries).
// If a full ADC library is available on the platform it will override this.

#endif // ADC_H
